const express = require('express');const router = express.Router();const auth = require('../middleware/auth');const doctorAuth = require('../middleware/doctorAuth');const {    getDashboard,    getProfile,    updateProfile,    updateSettings,    getAppointments,    updateAppointmentStatus,    getPrescriptions,    createPrescription,    getPatients} = require('../controllers/doctorController');router.get('/dashboard', auth, doctorAuth, getDashboard);router.get('/profile', auth, doctorAuth, getProfile);router.put('/profile', auth, doctorAuth, updateProfile);router.put('/settings', auth, doctorAuth, updateSettings);router.get('/appointments', auth, doctorAuth, getAppointments);router.put('/appointments/:id/status', auth, doctorAuth, updateAppointmentStatus);router.get('/prescriptions', auth, doctorAuth, getPrescriptions);router.post('/prescriptions', auth, doctorAuth, createPrescription);router.get('/patients', auth, doctorAuth, getPatients);module.exports = router; 