const express = require('express');const router = express.Router();const auth = require('../middleware/auth');const adminAuth = require('../middleware/adminAuth');const {    getSettings,    updateProfile,    updateNotificationSettings,    changePassword,    getAppointments,    updateAppointmentStatus,    updateAppointment,    deleteAppointment} = require('../controllers/adminController');router.get('/settings', auth, adminAuth, getSettings);router.put('/profile', auth, adminAuth, updateProfile);router.put('/notification-settings', auth, adminAuth, updateNotificationSettings);router.put('/change-password', auth, adminAuth, changePassword);router.get('/appointments', auth, adminAuth, getAppointments);router.put('/appointments/:id/status', auth, adminAuth, updateAppointmentStatus);router.put('/appointments/:id', auth, adminAuth, updateAppointment);router.delete('/appointments/:id', auth, adminAuth, deleteAppointment);module.exports = router; 